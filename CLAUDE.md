# cortex

Cortex desktop app (Tauri). This file marks the repo as a Cortex project
(`has_claude_md`) alongside its `.git` repo and `.cortex/rules/` directory.

Add project-specific instructions, conventions, and context below.
